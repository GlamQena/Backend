//TODO => 
// storeOwner or admin can't delete profile directly but sent request until a responsible admin approve it
//*store owner whose store has pending or preparing orders from can't leave 
// mark the storeOwner or admin document properties deletion_requested as true and deletion_Status as pending (default)